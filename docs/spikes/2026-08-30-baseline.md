# Baseline: pre-refactor build cost

Measured on WSL2 desktop, 12 cores, 45 GB RAM, 2026-08-30, against the
pre-refactor Dockerfile (from-source Boost via `Dockerfile.boost` +
`boost-provider` stage, `deb-collector` stage).
**The pre-refactor build did not complete** — see "Finding" below.

Source of the timing: local build, but the underlying log for that build
was subsequently overwritten by a second attempt (itself cancelled before
finishing) before it could be saved anywhere durable. The timing and
error-text figures below were read live off that first, now-gone log and
transcribed into this document — they are not re-derivable from anything
that currently exists on disk. **Read "Evidentiary status of the claims in
this document" immediately below before treating any number here as
independently checkable.**

## Evidentiary status of the claims in this document

**Independently verifiable today** — any reader can re-check these without
running a build:

- `Dockerfile.boost` line 18: `ARG BOOST_LIBS_TO_BUILD=iostreams,regex,serialization,system`
  — `program_options` is absent. Read the file.
- `main`'s commit `fba509a fix: build program_options boost lib` — `git show fba509a`.
- `postgres:17.2-bookworm` base image size, 434725324 bytes —
  `docker image inspect postgres:17.2-bookworm --format '{{.Size}}'`.
- The Boost image rebuilt on this host mid-investigation:
  `boost:bookworm-1.85.0`, image ID
  `sha256:38aad5c973d2e7624ef094359e730dabbdcae476bd99c7d03de6fc3e3abbc822`,
  created `2026-08-30T16:16:54-07:00`, size 85920208 bytes —
  `docker image inspect boost:bookworm-1.85.0`. This is evidence that a
  patched rebuild was attempted, not evidence of the original failure
  described below.

**Not reproducible from surviving artifacts** — read live during the
original build by the person running it, then lost when a second attempt
overwrote the log; treat these as reported, not as independently
checkable, and do not expect to find them by grepping this repository or
its build logs:

- The 1191 s wall-clock figure.
- The `[100%]` / `Built target testCIPLabeler` detail.
- The quoted `gaExample` linker error lines and the
  `make: *** [Makefile:166: all] Error 2` block, below.
- The load average 223 reading from the earlier `NUM_BUILD_CORES=10`
  attempt.

Why these are unrecoverable: attempt 1 (`baseline-build.sh`) built with
`--no-cache`, wrote its output to `runtime-build.log`, and computed
`BASELINE_WALL_SECONDS=1191` from `date +%s` deltas taken around the
build, saved into `baseline-result.env`. Attempt 2 (`baseline-build2.sh`),
which patched `Dockerfile.boost` to add `program_options` and tried again,
reused that exact same log path and overwrote it with `>` — and attempt 2
was itself killed before finishing, at 64%. `runtime-build.log` on disk
today is attempt 2's cancelled run, not attempt 1's failure; it does not
contain the errors quoted below, and neither `testCIPLabeler` nor
`gaExample` will be found by searching it.
`BASELINE_ATTEMPT1_WALL_SECONDS=1191` in `baseline-result.env` is a manual
transcription of the number attempt 1 had already computed and displayed
— written down when attempt 2's script was authored — not a fresh
computation, and there is no surviving log to recompute it from.

## Finding: the pre-refactor state does not build

A cold (`--no-cache`) build of the committed `Dockerfile` against the
untracked `Dockerfile.boost`, exactly as both exist in this worktree, was
observed to fail before producing a runtime image. It reached `[100%]` of
the RDKit `make` step and then failed linking the `gaExample` binary with
an undefined reference to `boost::program_options`. **The quotes below are
transcribed from the original run's log, which no longer exists on disk —
see "Evidentiary status", above; they are reported, not independently
reproducible from anything currently in this repository or its build
artifacts.** Representative linker errors (there were dozens; a few are
enough to show the shape of the failure):

```
/usr/bin/ld: CMakeFiles/gaExample.dir/GaExample.cpp.o: in function `boost::program_options::validation_error::~validation_error()':
GaExample.cpp:(.text._ZN5boost15program_options16validation_errorD2Ev[...]+0x3): undefined reference to `vtable for boost::program_options::error_with_option_name'
/usr/bin/ld: GaExample.cpp:(.text.startup+0x3e3): undefined reference to `boost::program_options::options_description::m_default_line_length'
/usr/bin/ld: GaExample.cpp:(.text.startup+0x447): undefined reference to `boost::program_options::options_description::add_options()'
```

`make` runs in parallel, so the individual link failure above scrolls past
long before the build actually stops; the top-level `all` target only
reports failure once every other parallel job has drained, at the very end
of the log:

```
#16 1113.3 make: *** [Makefile:166: all] Error 2
#16 ERROR: process "/bin/sh -c make" did not complete successfully: exit code: 2
------
 > [builder  9/11] RUN --mount=type=cache,target=/tmp/rdkit-build,id=rdkit-build,uid=999,gid=999     make:
------
Dockerfile:114
--------------------
 113 |     WORKDIR ${BUILD_DIR}
 114 | >>> RUN --mount=type=cache,target=/tmp/rdkit-build,id=rdkit-build,uid=999,gid=999 \
 115 | >>>     make
 116 |     RUN --mount=type=cache,target=/tmp/rdkit-build,id=rdkit-build,uid=999,gid=999 \
--------------------
ERROR: failed to solve: process "/bin/sh -c make" did not complete successfully: exit code: 2
```

A reader looking for "the error" at the end of the log will find only this
generic `Error 2` / exit-code-2 wrapper — the actual cause is the
`program_options` linker errors earlier in the same step's output, not
anything near this final summary.

**Root cause — and this part does not depend on the lost log.** Two facts,
both independently checkable right now (see "Evidentiary status", above),
are sufficient on their own to establish that the pre-refactor tree cannot
build, regardless of whether the transcribed failure output above is
trusted:

1. `Dockerfile.boost` declares

   ```
   ARG BOOST_LIBS_TO_BUILD=iostreams,regex,serialization,system
   ```

   — `program_options` is not in that list, so the from-source Boost image
   this Dockerfile produces never builds `libboost_program_options`.
2. RDKit's own build configuration links its `gaExample` target (built
   whenever `RDK_BUILD_PGSQL`/the default target set is built, as it is
   here) against `Boost::program_options`. A Boost install missing that
   library therefore cannot satisfy RDKit's own link step for that target
   — independent of any specific run's log.

`main`'s history contains a commit that specifically fixed the omission
(`fba509a fix: build program_options boost lib`), confirming this is a
known, previously-fixed problem that the wip `Dockerfile.boost` in this
worktree does not carry.

This is direct, empirical evidence for R2 (swap from-source Boost for
Debian's `libboost1.81-dev` package): the failure exists only because the
from-source build is a hand-maintained, incomplete enumeration of which
Boost libraries to compile. Debian's `libboost1.81-dev` family ships every
Boost library, including `program_options`, as a single package — "which
Boost libraries do we compile" stops being a question that can be gotten
wrong once Boost comes from `apt` instead of from source.

## Measured cost of the pre-refactor RDKit compile

**Not independently reproducible — see "Evidentiary status", above.** The
figures in this section were read live during attempt 1's build and
transcribed afterward; the log they came from was later overwritten by a
cancelled second attempt, so there is nothing on disk today to re-derive
them from.

| pair | PostgreSQL 17.2 / RDKit 2024_09_5 |
| --- | --- |
| wall clock to `[100%]` of `make`, then link failure | 1191 s |
| `NUM_BUILD_CORES` | 4 |
| `MAKEFLAGS` | `-j4 -l12` |
| BuildKit cache | cold (`docker build --no-cache`) |
| runtime image produced | **no** |

1191 s is a **lower bound on a full pre-refactor build, not a completed
build** — but a tight one. The failure surfaces at `[100%]`: every RDKit
object had already compiled and `make` had already reported
`Built target testCIPLabeler` before the `gaExample` link failed, so the
1191 s figure covers essentially the entire compile. Only the final link
of one *example* binary, `make install`, `pgsql_install.sh`, the
`deb-collector` stage, and the `runtime` stage's own layer cost are
missing from it — which is what makes it a meaningful lower bound rather
than a partial number. No pre-refactor runtime image was produced at any
point in this investigation with the as-committed `Dockerfile.boost`.

## Why `-j4 -l12`

**Correction (Task 5 code review):** this section originally explained the
load-223 event below as `-j` multiplying across CMake's recursive `make`
invocations. That explanation was wrong, and Task 5's Dockerfile fix landed
on the real mechanism, not that one — recorded here so the two don't
diverge.

The actual cause: `ARG MAKEFLAGS='-j${NUM_BUILD_CORES}'` (as it stood in the
Dockerfile before Task 5) is an ARG whose *default value* references another
ARG. Docker does not expand `${...}` references inside another ARG's
default value — that substitution only happens inside instruction text like
`RUN`. So `MAKEFLAGS` reached the build environment as the **literal
string** `-j${NUM_BUILD_CORES}`, unexpanded placeholder and all. GNU `make`
parses an unrecognized/malformed numeric argument to `-j` as bare `-j`,
which means **unlimited parallel jobs**, not "throttled by
`NUM_BUILD_CORES`." This is consistent with everything observed: the run
that hit load average 223 passed `NUM_BUILD_CORES=10` with no explicit
`MAKEFLAGS` build-arg (so the Dockerfile's own broken default applied and
handed `make` unbounded parallelism), while every run that passed an
explicit `--build-arg MAKEFLAGS='-j4 -l12'` on the command line (which
*does* get substituted, since build-args set the ARG's value directly
rather than going through its default-value expansion) sat at load ~4 as
intended. There is no recursion-depth multiplication involved.

**The fix**, landed in Task 5 (`feat: build Boost from Debian packages,
delete deb-collector (R2, R3)` and its follow-up review-fix commit): the
`ARG MAKEFLAGS` indirection is gone entirely. `make -j${NUM_BUILD_CORES}`
and `make -j${NUM_BUILD_CORES} install` now pass the job count directly on
the `make` command line inside `RUN`, where ARG substitution does work, and
where `-j` on the command line additionally gives nested `make` invocations
a real shared jobserver — something the environment-variable form never
provided at all, correct expansion or not.

The value used for the 1191 s figure above is `NUM_BUILD_CORES=4` plus an
explicit `--build-arg MAKEFLAGS='-j4 -l12'`, where `-l12` is a load-average
cap (GNU Make's `-l` flag: don't start new jobs when load average exceeds
this) that further contains parallelism regardless of the `-j` value voted
by `NUM_BUILD_CORES`. Since Task 5's fix, the `MAKEFLAGS` build-arg is no
longer consumed by the Dockerfile at all (harmless if still passed; Docker
just warns it wasn't used) — the throttle is `NUM_BUILD_CORES` alone now.
**Any later timing comparison against this number must use the identical
`NUM_BUILD_CORES=4` throttle, or the ratio is meaningless.**

## R7 acceptance threshold (SPEC 4.7)

R7 requires a cartridge-only rebuild to be at least 5x faster than a full
build. This baseline document does **not** set that threshold. The
pre-refactor full build never completed (see Finding, above), and even if
it had, using it as the denominator would be the wrong choice: a
pre-refactor full build includes a from-source Boost compile that Task 17's
post-refactor build no longer has to do, which would inflate the
denominator and make the 5x gate artificially easy to pass. The honest
denominator is a full build of the **post-refactor** Dockerfile that Tasks
5/9/12 produce. Task 17 measures that build directly and derives the R7
threshold from it.

## Acceptance criterion 4 (size)

| image | size |
| --- | --- |
| `postgres:17.2-bookworm` base | 434725324 bytes |
| pre-refactor `postgres-rdkit:pg17.2-rdkit-2024_09_5` runtime image | **PENDING** |

The pre-refactor runtime image was never built locally (see Finding,
above), so its size cannot be measured from this host. To fill in the
PENDING value, inspect the previously-published image in the registry:

```bash
docker buildx imagetools inspect ghcr.io/radusuciu/docker-postgres-rdkit/postgres-rdkit:<tag>
```

`<tag>` above is **intentionally left unresolved**, not an unfilled
template placeholder this document forgot to complete: anonymous access to
that repository path returned "not found" during this investigation, so no
tag could be confirmed to exist there at all. Filling in this row needs
either registry authentication or the repository owner to supply a
known-good tag; do not guess one.

## Boost image note

`boost:bookworm-1.85.0` on this host was rebuilt during this investigation
with `BOOST_LIBS_TO_BUILD` locally patched to include `program_options`
(to test whether the link failure above was the only blocker). That local
image **now includes `program_options`** and so differs from what the
committed `Dockerfile.boost` — `BOOST_LIBS_TO_BUILD=iostreams,regex,serialization,system`,
confirmed restored to its original, unpatched contents after this
investigation — would produce if rebuilt from scratch. Anyone re-running
this baseline, or any later task that expects `boost:bookworm-1.85.0` to
match the committed `Dockerfile.boost`, should rebuild it locally first
rather than trusting the tag already on this host.

## After Boost swap (Task 5)

Measured on the same host, same day, against the post-refactor `Dockerfile`
(Debian `libboost1.74-dev` family via `scripts/install_boost.sh`, no
`boost-provider`/`deb-collector` stages):

| image | size |
| --- | --- |
| `postgres:17.2-bookworm` base | 434725324 bytes |
| post-refactor `postgres-rdkit:pg17.2-rdkit-2024_09_5` runtime image | 493438152 bytes (493MB, `docker images` display size) |

This is observational only (R3: minimality is soft, no size gate). There is
still no pre-refactor runtime image size to compare against — the
"PENDING" row above was never resolved, since the pre-refactor tree never
produced a runtime image locally and no registry tag could be confirmed
(see "Acceptance criterion 4 (size)", above). Do not backfill that PENDING
value with this number or any other invented figure; the honest state is
that no before/after size delta can be computed from anything measured in
this repository's history, only the post-refactor number stands on its
own.

## Verification

Task 19 (final task) — acceptance pass against SPEC §6's ten criteria, run
2026-08-31 in worktree `refactor-single-branch-matrix` at commit
`731c52f`. Per controller corrections C1–C12 on the Task 19 brief: Step 2
(merge to `main` and push) and Step 3 (registry/CI checks against a live
`gh workflow run`) were **not executed** — that decision belongs to the
repository owner, outside this session. `versions.json` holds
`rdkit_versions: ["2026_03_6", "2025_09_6"]`; every `2025_09_2` literal in
the original brief is substituted with `2026_03_6` or `2025_09_6` below
per C4 (`2025_09_2` cannot build — unguarded `Descriptors::GETAWAY`).

This is an honest record, not a scorecard: every criterion below is marked
PASSED only where I observed the command's output myself in this session
(shown inline) or can cite a specific earlier task report that recorded
it; PENDING where it depends on a registry or CI run this session cannot
perform, with the exact command that closes it; or AMENDED-BY-OWNER where
the repository owner changed the requirement.

**1. `main` builds every matrix entry from `versions.json` with no branch checkouts — PASSED.**
Ran myself:
```
$ grep -c 'ref:' .github/workflows/build.yml
0
$ scripts/matrix.py --format json | python3 -c 'import json,sys; print(len(json.load(sys.stdin)), "entries")'
10 entries
```
10 = 5 PostgreSQL majors (14, 15, 16, 17, 18) × 2 RDKit versions
(2026_03_6, 2025_09_6), matching `versions.json`'s current contents.

**2. No image in the build path compiles Boost from source — PASSED.**
Ran myself:
```
$ grep -rniE 'b2 |bootstrap\.sh|boost_[0-9]+_[0-9]+_[0-9]+\.tar|debhelper' Dockerfile Dockerfile.rdkit-core
clean
```

**3. The Boost preflight fails with its specific message below the floor — PASSED.**
Ran myself:
```
$ bash tests/test_install_boost.sh
...
--- bookworm, floor 9.99.0: expect the R2 preflight failure ---
ok: unsatisfiable floor exits 1
ok: error names the floor
ok: error lists what the suite offers
ok: error tells the operator to change the suite

test_install_boost.sh: 12 assertions, 0 failed
```

**4. The runtime image is not materially larger than the image it replaces — PENDING (partially answered; see below).**
Per C6: this criterion wants the size of the PUBLISHED image it replaces,
and `ghcr.io/radusuciu/docker-postgres-rdkit/postgres-rdkit` could not be
read anonymously (recorded above, "Acceptance criterion 4 (size)"). That
half stays PENDING. Closing command, once the owner supplies registry
auth or a known-good tag:
```
docker buildx imagetools inspect ghcr.io/radusuciu/docker-postgres-rdkit/postgres-rdkit:<tag>
```
What CAN be measured locally (C7) — read myself in this session with
`docker image inspect <tag> --format '{{.Size}}'`, all three images
present on this host:

| image | tag | size |
| --- | --- | --- |
| base | `postgres:17.2-bookworm` | 434725324 bytes |
| after Boost swap (Task 5, R2) | `postgres-rdkit:pg17.2-rdkit-2024_09_5` | 493438152 bytes |
| final (post-R7 core reuse, Task 18) | `postgres-rdkit:postgres-17.11-rdkit-2026_03_6` | 456025745 bytes |

The final image is *smaller* than the Task 5 (post-Boost-swap,
pre-R7) image — 456025745 vs. 493438152 bytes, a ~37MB reduction — and
both are in the same order of magnitude as the base PostgreSQL image
(+~5–14%), not materially larger by any informal reading. There is still
no pre-refactor (from-source-Boost) runtime image size on record to
compare against, and no published registry image was reachable, so the
comparison this criterion actually asks for ("the image it replaces")
remains open.

**5. Every image carries all R9 labels; pickle-version matches SPEC §3.2 — PASSED, with a spec-staleness note.**
Ran myself against the two tags `make runtime` currently produces on this
host for the two shipping RDKit families (major-based tag, default suite,
per C5):
```
$ docker image inspect postgres-rdkit:postgres-17.11-rdkit-2026_03_6 --format '{{json .Config.Labels}}' | python3 -m json.tool
{
    "org.boost.version": "1.81.0",
    "org.debian.suite": "bookworm",
    "org.opencontainers.image.revision": "ba7d65fadb86e666b30c6896cb378a01c8d0c72d",
    "org.opencontainers.image.source": "https://github.com/radusuciu/docker-postgres-rdkit",
    "org.postgresql.base-digest": "sha256:051f7b7b3abdd564d5d1bd1e8c4b9c1b6e77087d1dd22020ede611c096a272e0",
    "org.postgresql.version": "17.11",
    "org.rdkit.cartridge-version": "4.8.0",
    "org.rdkit.pickle-version": "16.3.0",
    "org.rdkit.version": "2026_03_6"
}
$ docker image inspect postgres-rdkit:postgres-17.11-rdkit-2025_09_6 --format '{{json .Config.Labels}}' | python3 -m json.tool
{
    "org.boost.version": "1.81.0",
    "org.debian.suite": "bookworm",
    "org.opencontainers.image.revision": "b1032629901505b942d07df81ab93f62605d23dc",
    "org.opencontainers.image.source": "https://github.com/radusuciu/docker-postgres-rdkit",
    "org.postgresql.base-digest": "sha256:051f7b7b3abdd564d5d1bd1e8c4b9c1b6e77087d1dd22020ede611c096a272e0",
    "org.postgresql.version": "17.11",
    "org.rdkit.cartridge-version": "4.8.0",
    "org.rdkit.pickle-version": "16.2.0",
    "org.rdkit.version": "2025_09_6"
}
```
All 8 labels R9 requires (§4.9) are present on both, none empty.
`2025_09_6`'s `pickle-version` (16.2.0) matches SPEC §3.2's table row for
the 2025_09 family exactly. `2026_03_6`'s `pickle-version` (16.3.0) is not
in SPEC §3.2's table at all — the table only goes up to 2025_09 → 16.2.0
— but the label is derived mechanically from RDKit's own
`MolPickler::versionMajor/Minor/Patch` (R9, §4.9), not from the table, so
this is a spec-staleness gap, not a label defect. **Spec-staleness item
(carried forward per C9):** SPEC §3.2 says patch drift within a
pickle-version family (e.g. 2025_03 vs 2025_09, both 16.2.0) is safe, and
that framing no longer generalises now that the matrix ships two RDKit
families whose pickle versions actually differ — 2026_03 is 16.3.0 against
2025_09's 16.2.0. §3.2's own rule ("the client's RDKit must be >= the
cartridge's RDKit") still applies and the mismatch is exactly what the
label exists to make mechanically checkable, but the table itself should
be extended to cover 2026_03 and the "safe to mix" note qualified to say
*which* pairs are safe, not asserted as a blanket property of patch drift.

**6. The exact and major-version tags resolve to the same digest — PENDING (registry/CI-dependent).**
Per C2/Ruling 34: depends on a CI run against GHCR that has not happened
and cannot happen in this session (`git push`/`gh workflow run` are out of
scope per C1, and anonymous GHCR reads of this repository failed during
Task 1's investigation). Not recorded as passed, not recorded as failed —
it was PENDING before this work started. Closing commands, once a
workflow run has pushed images:
```
IMAGE=ghcr.io/radusuciu/docker-postgres-rdkit/postgres-rdkit
a=$(docker buildx imagetools inspect "$IMAGE:postgres-17.11-rdkit-2025_09_6" --format '{{.Manifest.Digest}}')
b=$(docker buildx imagetools inspect "$IMAGE:postgres-17-rdkit-2025_09_6"    --format '{{.Manifest.Digest}}')
[ "$a" = "$b" ] && echo "PASS 6: same digest" || echo "FAIL 6: $a != $b"
```

**7. Re-running the workflow with no upstream/repository change performs zero image builds — PENDING (registry/CI-dependent).**
Same reason as criterion 6 (C2). Closing commands:
```
gh workflow run build.yml
gh run watch
# Confirm resolve-matrix logs "Nothing to build" and the build job is skipped.
```

**7a. An on-demand build of an older point release leaves the major tag alone — PENDING (registry/CI-dependent).**
Same reason as criterion 6 (C2). Closing commands:
```
IMAGE=ghcr.io/radusuciu/docker-postgres-rdkit/postgres-rdkit
before=$(docker buildx imagetools inspect "$IMAGE:postgres-17-rdkit-2025_09_6" --format '{{.Manifest.Digest}}')
gh workflow run build.yml -f postgres=17.9 -f rdkit=2025_09_6
gh run watch
after=$(docker buildx imagetools inspect "$IMAGE:postgres-17-rdkit-2025_09_6" --format '{{.Manifest.Digest}}')
[ "$before" = "$after" ] && echo "PASS 7a: moving tag did not move backwards" || echo "FAIL 7a"
docker buildx imagetools inspect "$IMAGE:postgres-17.9-rdkit-2025_09_6" >/dev/null && echo "PASS 7a: point tag was pushed"
```

**7b. `DEBIAN=trixie` succeeds with no file edit — PASSED (cited, not re-run per C5/C11).**
Task 12's report
(`.superpowers/sdd/2026-08-30-single-branch-build-matrix/task-12-report.md`,
"Step 6") ran `make runtime POSTGRES=17 RDKIT=2025_09_6 DEBIAN=trixie` to
completion (~23 min cold trixie compile, log
`/tmp/task12-step6-trixie-1.log`), recorded the Boost-family selection
line `Selected Boost family 1.83 (floor 1.81.0)`, produced
`postgres-rdkit:postgres-17.11-rdkit-2025_09_6-trixie` (471488448 bytes,
suite-suffixed per Ruling 36), and confirmed via `git status --porcelain`
that no tracked file was modified by the build. That image is still
present on this host (`docker images` shows
`postgres-rdkit:postgres-17.11-rdkit-2025_09_6-trixie`, 471MB) — I did not
rebuild it.

**8. `test-build`, `test-runtime`, and the R8 smoke test pass — PASSED (cited for representative pairs, not re-run per C5/C11; scope caveat below).**
Task 10's report
(`.superpowers/sdd/2026-08-30-single-branch-build-matrix/task-10-report.md`)
recorded `test_smoke_test.sh: 7 assertions, 0 failed` against
`postgres-rdkit:postgres-17-rdkit-2026_03_6`. Task 12's report
("Step 5") recorded `make smoke POSTGRES=17.11 RDKIT=2025_09_6` passing
end-to-end (`SMOKE TEST PASSED: postgres-rdkit:postgres-17.11-rdkit-2025_09_6`),
which itself depends on `test-build`/`test-runtime` succeeding as
prerequisites of `runtime`/`smoke` in the Makefile. I additionally re-ran
`make test-scripts` myself this session (see criterion-9-adjacent note
below) and it included a fresh, passing `test_smoke_test.sh` run against
the still-present `postgres-rdkit:postgres-17-rdkit-2026_03_6` image.
**Scope caveat, stated honestly:** these runs cover two representative
matrix pairs (one per RDKit family) plus one non-default-suite build, not
literally all 10 matrix entries — no session has run `test-build` +
`test-runtime` + the smoke test across the full cross product. `.github/workflows/build.yml`
does wire `test-build`, `test-runtime`, and `scripts/smoke_test.sh` into
the per-matrix-entry `build` job (confirmed by reading the workflow file:
`target: test-build`, `target: test-runtime`, and a `Functional smoke
test` step calling `scripts/smoke_test.sh` all appear inside the job that
iterates `strategy.matrix`), so once the workflow actually runs, every
entry gets this treatment — but that execution is exactly what criteria
6/7/7a are PENDING on, and I did not additionally re-verify it here.

**9. A single-line edit to `versions.json` adds or removes a version pair, with no other file changed — PASSED.**
Ran myself (edited an in-memory copy, wrote only `/tmp/v2.json`, never
touched the tracked file — confirmed per C10):
```
$ python3 -c "
import json, subprocess
c = json.load(open('versions.json'))
before = len(json.loads(subprocess.run(['scripts/matrix.py','--format','json'],capture_output=True,text=True).stdout))
c['postgres_majors'].append('19')
json.dump(c, open('/tmp/v2.json','w'))
after = len(json.loads(subprocess.run(['scripts/matrix.py','--file','/tmp/v2.json','--format','json'],capture_output=True,text=True).stdout))
print(f'{before} -> {after} entries from one list edit')
"
10 -> 12 entries from one list edit
$ git status --porcelain versions.json
(empty)
```

**10. `make runtime POSTGRES=17.11 RDKIT=2023_09_6` builds and passes R8, with no `versions.json` edit, and the equivalent `workflow_dispatch` run pushes the same tags — AMENDED-BY-OWNER.**
Per C3/Ruling 34: the repository owner ruled RDKit `2023_09_6` "too old to
care about," and Task 12 (Step 7 in its brief) dropped the corresponding
build step; it was never run in this session either (confirmed: no
`2023_09_6` image exists in `docker images` on this host). What still
demonstrates R11's on-demand capability without it:
- Task 12 Step 3 pinned a point release (`make runtime POSTGRES=17.11
  RDKIT=2025_09_6`) with no `versions.json` edit, producing
  `postgres-rdkit:postgres-17.11-rdkit-2025_09_6` (455485037 bytes).
- Task 12 Step 6 (criterion 7b, above) overrode the whole suite
  (`DEBIAN=trixie`), also with no `versions.json` edit.
- Boost-family SELECTION — the one thing a `2023_09_6` build would have
  uniquely proven, since its floor (1.58) selects bookworm's 1.74 family
  rather than the 1.81 family the matrix's current entries all select —
  is covered by Task 3's unit tests (`tests/test_install_boost.sh`,
  re-run myself under criterion 3 above), which exercise floors 1.58,
  1.81, and 9.99 directly in throwaway Debian containers.
**Residual, stated honestly:** no build in this project, in this session
or any recorded earlier one, exercises a Boost family other than 1.81
end to end inside a real `postgres-rdkit` image. The `1.74` selection path
is proven only at the `install_boost.sh` unit-test level, not inside a
full runtime image build. The `workflow_dispatch` half of this criterion
is additionally CI-dependent in the same way as criteria 6/7/7a and was
never in scope for this amended criterion either.

### R7 (RDKit core reuse) — implemented and verified locally

Not one of the ten numbered criteria, but requested by C8/the addendum
since R7 rows changed status mid-project. Read (not re-run, per C5/C11)
`docs/spikes/2026-08-31-rdkit-core-reuse.md`: the spike measured a full
cold build at 1533s against a cartridge-only rebuild at 66s (23.2x
end-to-end speedup, 115.2x on the compile step alone; 3 objects rebuilt in
the spike's minimal consumer), verdict **PASS** against R7's 5x
threshold, decision "implement R7 (Task 18)". Task 18's report
(`.superpowers/sdd/2026-08-30-single-branch-build-matrix/task-18-report.md`)
records the integrated build rebuilding exactly **15 objects** (3 CXX + 12
C) — matching the spike's object-level reuse — and producing
`postgres-rdkit:postgres-17.11-rdkit-2026_03_6` at 456025745 bytes, the
same figure I read directly off this host under criterion 4 above. R7
is implemented and verified, not spike-gated or dropped.

### Other spec-staleness item carried forward (C9)

SPEC R4's tag shape (§4.4) is amended by **Ruling 36** (owner-directed,
recorded in `progress.md` and implemented in Tasks 12/14/15): a build
whose `debian` differs from `versions.json`'s own `debian` gets a
`-<suite>` suffix on its point-release tag and receives **no** moving
major tag and **no** `latest` tag, regardless of whether it is the
current point release. SPEC.md itself has not been updated to reflect
this ruling; R4 as written describes only the default-suite tag shape.
The trixie build under criterion 7b above (tag
`postgres-17.11-rdkit-2025_09_6-trixie`) is a live example of the amended
shape.

### Whole script suite (`make test-scripts`)

Ran myself, full clean run, this session:
```
$ make test-scripts
...
test_build_key.sh: 12 assertions, 0 failed
test_install_boost.sh: 12 assertions, 0 failed
test_rdkit_labels.sh: 8 assertions, 0 failed
test_resolve_matrix.sh: 28 assertions, 0 failed
test_resolve_pg.sh: 18 assertions, 0 failed
test_runtime_packages.sh: 10 assertions, 0 failed
test_smoke_test.sh: 7 assertions, 0 failed
=== tests/test_matrix.py ===
Ran 15 tests in 0.063s
OK
```
Exit code 0, 95 shell assertions + 15 Python tests, 0 failures. This is
one point in a KNOWN non-deterministic range: several suite files depend
on live Docker Hub pulls, and the total assertion count and pass/fail
outcome have varied between runs on an unchanged suite (89 and 87
assertions with 2 failures both observed previously, per the addendum);
this run happened to land fully green with 95 shell assertions. Not
recorded as a stable gate — CI does not run `make test-scripts` at all
(a decision already recorded for the whole-branch review, referenced but
not relitigated here).

### Summary

| # | criterion | verdict |
| --- | --- | --- |
| 1 | matrix build, no branch checkouts | PASSED |
| 2 | no from-source Boost | PASSED |
| 3 | Boost preflight failure message | PASSED |
| 4 | runtime image not materially larger | PENDING (registry half); local sizes measured |
| 5 | R9 labels + pickle-version | PASSED (spec-staleness note) |
| 6 | exact/major tag same digest | PENDING (registry/CI-dependent) |
| 7 | no-op re-run builds nothing | PENDING (registry/CI-dependent) |
| 7a | on-demand old point release leaves major tag alone | PENDING (registry/CI-dependent) |
| 7b | `DEBIAN=trixie` needs no file edit | PASSED (cited) |
| 8 | test-build/test-runtime/R8 smoke pass | PASSED (cited, scope caveat) |
| 9 | one-line `versions.json` edit changes matrix size | PASSED |
| 10 | `2023_09_6` on-demand build | AMENDED-BY-OWNER |
